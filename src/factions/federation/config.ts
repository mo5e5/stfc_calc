import type { StandardFactionConfig } from "../shared/types";

export const config: StandardFactionConfig = {
  key:        "Federation",
  armadaType: "Explorer",
  cssVar:     "var(--federation)",
};
